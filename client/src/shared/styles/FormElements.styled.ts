import { css } from "styled-components";

export const FormElementsShared = css`
  font-size: var(--font-size-small);
  font-weight: var(--font-weight-regular);
  padding: 0.5rem 1rem;
  border-radius: 5px;
  outline: none;
  border: none;
`;
